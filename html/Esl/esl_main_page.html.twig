{% extends 'homepage.html.twig' %}

{% block title %}Digi Tools - ESL{% endblock title %}

{% block main_column %}
  {% set app = globals.app %}
  <section id="submenu">
    <div id="registration">
      <h1>ESL menu</h1>
      <ul>
        <li><a href="{{ app.urlFor('esl_sheet_show') }}" class="form-control btn">lijst klanten</a></li>
          {% if globals.logged_in == 1 %}
          <li><a href="{{ app.urlFor('esl_sheet_new') }}" class="form-control btn">nieuwe klant</a></li>
          {% endif %}
      </ul>
    </div>
  </section>
{% endblock main_column %}